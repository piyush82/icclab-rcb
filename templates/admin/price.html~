{% extends "admin/base_site.html" %}
{% block head %}
<script type="text/javascript" src="/site_media/prototype.js"></script>
<script type="text/javascript" src="/site_media/my_ajax_function.js"></script>

{% endblock %}
{% load i18n %}
{% block content %}

<link rel="stylesheet" href="//code.jquery.com/ui/1.10.4/themes/smoothness/jquery-ui.css">
<script type="text/javascript" src="{{ STATIC_URL }}js/jquery-1.11.0.min.js"></script>
<script type="text/javascript" src="{{ STATIC_URL }}js/jquery-ui-1.10.4.min.js"></script>
<script type="text/javascript" src="{{ STATIC_URL }}js/ui.js"></script>
   {% if not is_popup %}
    <!-- Header Starts -->
    <div class="header">
      <div class="logo">
        <h1><a href="/index/"><img src="{{STATIC_URL}}logo.png" width="170" height="60" alt="icclab-logo" /></a><span>Reinventing RCB solutions for tomorrow!</span></h1>
      </div>
      <div class="header_nav">
        <ul>
          <li><a href="#">Home</a></li>
          <li><a href="#">The Lab</a></li>
          <li><a href="#">Services</a></li>
          <li><a href="#">Solutions</a></li>
          <li class="last"><a href="#">Contact us</a></li>
        </ul>
        <div class="clear"></div>
      </div>
      <div class="clear"></div>

        {% if user.is_active and user.is_staff %}
        <div id="user-tools">
            {% trans 'Welcome,' %}
            <strong>{% firstof user.get_short_name user.get_username %}</strong>.
            {% block userlinks %}
                {% url 'django-admindocs-docroot' as docsroot %}
                {% if docsroot %}
                    <a href="{{ docsroot }}">{% trans 'Documentation' %}</a> /
                {% endif %}
                {% if user.has_usable_password %}
                <a href="{% url 'admin:password_change' %}">{% trans 'Change password' %}</a> /
                {% endif %}
                <a href="{% url 'admin:logout' %}">{% trans 'Log out' %}</a>
            {% endblock %}
        </div>
        {% endif %}
    </div>
    <div class="banner"><img src="{{STATIC_URL}}banner.jpg" width="980" height="332" alt="banner" /></div>
    <!-- Header ends --> 
    
    {% block breadcrumbs %}
    <div class="breadcrumbs">
    <a href="{% url 'admin:index' %}">{% trans 'Home' %}</a>
    {% if title %} &rsaquo; {{ title }}{% endif %}
    </div>
    {% endblock %}
    {% endif %}
<p> Define the pricing function. </p>

<form action="" method="post">
{% csrf_token %}
	{% if form.errors %}
		{% for field in form %}
			{% for error in field.errors %}
            			<div class="alert alert-error">
                			<strong>{{ error|escape }}</strong>
            			</div>
        		{% endfor %}
    		{% endfor %}
    		{% for error in form.non_field_errors %}
        		<div class="alert alert-error">
            			<strong>{{ error|escape }}</strong>
        		</div>
    		{% endfor %}
	{% endif %}

	
	<p>List of available meters per resource.</p>
	<div id="meters" style="height:600px; display:inline-block; overflow-y:auto; margin-bottom:30px; background-color:#E6E6E6;">
	{% for i in resources %}
 		<ul>	
   		<li><b>{{i}}</b></li>
 				
		
		{% for j in meter_list %}
			{% if forloop.counter == forloop.parentloop.counter %}
				{% for k in j %}
 					<ul>
  			 		<li>{{k}}</li>
 					</ul>
				{% endfor %}
			{% endif %}
		{% endfor %}
		</ul>
	{% endfor %}
    </div>
    <input type="hidden" id="var_resources" name="var_resources" value="{{ resources }}">
    <input type="hidden" id="var_meters" name="var_meters" value="{{ meter_list }}">
    <table cellspacing="0" cellpadding="0" style="border:none; font-size:medium; margin-bottom:20px; border-collapse: collapse;">
    {% for fields in pricing_func_form %}
        {%if "selected action" not in fields.label %}
           {%if "Resources" not in fields.label %}
		{%if "Func"  in fields.label %}
		<tr style="border:0;"><td style="width:95px; margin-right:10px; padding: 0;">Pricing function</td><td style="font-size:12px;">{{fields}}</td><tr>
		{%else%}
		<tr style="border:0;"><td style="width:95px; margin-right:10px; padding: 0;">{{fields.label}} </td><td style="font-size:12px;"> {{fields}}</td><tr>
	{% endif %}
	{% endif %}
	{% endif %}
    {% endfor %}
    </table>
    <div id="results"></div>
    <p>The pricing function will be applied to the user with id:</p>

    <ul>{{ user|unordered_list }}</ul>

    <input type="hidden" name="action" value="add_pricing_func" />
    <input type="submit" name="add_pricing" value="Add pricing function" />
</form>

{% endblock %}

