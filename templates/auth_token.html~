{% extends "base.html" %}
{% block title %}Home{% endblock %}
{% block sidebar %}
    <ul>
        <li><a href="/">Home</a></li>

        {% if user.is_authenticated %}
            <li><a href="/accounts/logout">Logout</a></li>
        {% else %}
            <li><a href="/accounts/login">Login</a></li>
        {% endif %}{% endblock %}
{% block content %}
<div id="content">
    
    {% if user.is_active and user.is_staff %}
	{% if form.errors %}
    		<p class="error">Sorry, that are not valid creditentials. </p>
  	{% endif %}

  	<form action="" method="post">
    		{% csrf_token %}
    		<label for="user">Username:</label>
    		<input type="user" name="user" value="" id="user">
    		<label for="pass">Password:</label>
    		<input type="pass" name="pass" value="" id="pass">
    		<label for="domain">Domain:</label>
    		<input type="domain" name="domain" value="" id="domain">
    		<label for="project">Project:</label>
    		<input type="project" name="project" value="" id="project">

   		 <input type="submit" value="login" />
    		<input type="hidden" name="next" value="{{ next|escape }}" />
  	</form>
    	
    {% endif %}

    

</div>
{% endblock %}
